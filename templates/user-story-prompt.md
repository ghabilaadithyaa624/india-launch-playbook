# User story prompt

Copy this into your document tool and fill in the brackets.

```
As a [user type], I want to [action] so that [benefit]. It is complete
when: [observable condition 1], [condition 2], [condition 3]. If it fails,
the user sees [clear message] and can [recovery action].
```
